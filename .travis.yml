language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t kdanga/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kdanga/docker-react npm run test
